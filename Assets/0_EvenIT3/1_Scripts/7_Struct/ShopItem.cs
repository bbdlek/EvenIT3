using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ShopItemList
{
    NormalGacha1, NormalGacha5,SpecialGacha1, SpecialGacha5, ClockItem, MilkItem, MaskItem, Package1, Package2, Silver1000, Silver5000, Silver10000, Gold1, Gold10, Gold20, Energy
}

public class ShopItem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
